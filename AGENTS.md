## Resources

* raw paper is in `paper.md`
* public experiments data is in `exp_data`


## Env

* use uv to manange python virtual env
* Always use uv add instead of uv pip install
* Never use UV_CACHE_DIR, use the default global cache dir

## Dependency

* Prefer to use `psychopy` python package
* Remember to check the code in .venv to understand how to use installed python lib.

## Coding Style

Always be clean and tidy, always write lots of comments for your code.